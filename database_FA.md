# 🗄️ ساختار دیتابیس

---

## 👤 USERS (کاربران)

### هدف:
افراد واقعی سیستم (ساکن، دانشجو، کارمند)

| ستون | نوع | توضیح | مقادیر |
|------|-----|--------|--------|
| id | INTEGER | شناسه یکتا | Auto Increment |
| full_name | TEXT | نام کامل | "علی احمدی" |
| employee_id | TEXT | کد کاربر | "EMP001", "S12345" |
| status | INTEGER | وضعیت فعال بودن | 1 = فعال / 0 = غیرفعال |
| created_at | TEXT | زمان ثبت | datetime |

---

## 🔑 DEVICES (دستگاه‌ها)

### هدف:
دستگاه‌های ESP32 یا گیت‌های ورود

| ستون | نوع | توضیح | مقادیر |
|------|-----|--------|--------|
| id | INTEGER | شناسه دستگاه | Auto |
| device_name | TEXT | نام دستگاه | "گیت ورودی A" |
| api_key | TEXT | کلید امنیتی | "abc123xyz" |
| location | TEXT | محل نصب | "ورودی ساختمان" |
| active | INTEGER | وضعیت دستگاه | 1 / 0 |
| created_at | TEXT | زمان ساخت | datetime |

---

## 🪪 CARDS (کارت‌های RFID)

### هدف:
اتصال کارت به کاربر

| ستون | نوع | توضیح | مقادیر |
|------|-----|--------|--------|
| id | INTEGER | شناسه کارت | Auto |
| user_id | INTEGER | کاربر مرتبط | FK به USERS |
| uid | TEXT | کد کارت RFID | "A1B2C3D4" |
| active | INTEGER | وضعیت کارت | 1 / 0 |
| created_at | TEXT | زمان ثبت | datetime |

---

## 📊 ATTENDANCE (هسته سیستم)

### هدف:
ثبت تمام رفت و آمدها

| ستون | نوع | توضیح | مقادیر |
|------|-----|--------|--------|
| id | INTEGER | شناسه رکورد | Auto |
| user_id | INTEGER | کاربر | FK |
| device_id | INTEGER | دستگاه ثبت کننده | FK |
| uid | TEXT | UID خام کارت | "A1B2C3D4" |
| event | TEXT | نوع رویداد | لیست پایین |
| created_at | TEXT | زمان ثبت | datetime |

---

### 📌 انواع رویداد (Event)

#### 🟢 ورود و خروج اصلی

check_in → ورود
check_out → خروج


---

#### 🟡 استراحت

break_start → شروع استراحت
break_end → پایان استراحت


---

#### 🏠 مدیریت پانسیون / رفت و آمد

room_in → ورود به اتاق
room_out → خروج از اتاق
temporary_leave → خروج موقت
return → بازگشت


---

## 🪵 LOGS (لاگ سیستم)

### هدف:
ثبت خطاها و فعالیت‌های سیستم

| ستون | نوع | توضیح | مقادیر |
|------|-----|--------|--------|
| id | INTEGER | شناسه | Auto |
| device_id | INTEGER | دستگاه مربوطه | FK |
| message | TEXT | پیام | "invalid api key" |
| level | TEXT | سطح | info / warn / error |
| created_at | TEXT | زمان ثبت | datetime |

---

# 🔗 ارتباط بین جداول


کاربران(USERS)

├── کارت‌ها (CARDS) →یک کاربر چند کارت دارد

└── حضور و غیاب (ATTENDANCE)

دستگاه‌ها(DEVICES)

└── حضور و غیاب (ATTENDANCE)


لاگ‌ها (LOGS)

└── دستگاه‌ها


---

# ⚙️ منطق عملکرد سیستم

دستگاه ESP32 درخواست ارسال می‌کند
سیستم API Key را بررسی می‌کند
دستگاه شناسایی می‌شود
UID کارت دریافت می‌شود
کاربر از روی کارت پیدا می‌شود
رویداد ثبت می‌شود
اطلاعات در دیتابیس ذخیره می‌شود

---

# 🏢 قابلیت‌های سیستم

✔ ثبت حضور و غیاب هوشمند  
✔ پشتیبانی از چند دستگاه  
✔ مدیریت کاربران  
✔ مدیریت کارت‌های RFID  
✔ ثبت استراحت و رفت و آمد  
✔ امنیت با API Key  
✔ قابل توسعه برای سیستم‌های واقعی  

---

# 🚀 توسعه‌های آینده

- پنل مدیریت (Dashboard)
- گزارش‌گیری روزانه و ماهانه
- محاسبه ساعت حضور واقعی
- تحلیل رفت و آمد
- اتصال واقعی ESP32 + RFID
- اپلیکیشن موبایل

---

# 🧠 جمع‌بندی

این سیستم فقط یک حضور و غیاب ساده نیست.

بلکه یک **سیستم کامل مدیریت رفت و آمد هوشمند** است که برای:

- پانسیون
- خوابگاه
- شرکت‌ها
- ساختمان‌های هوشمند

قابل استفاده و توسعه است.
